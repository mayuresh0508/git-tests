name: Test Print

on:
  push:
  workflow_dispatch:

jobs:
  print-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Print test 1
        run: echo "test 1"

      - name: Print test 2
        run: echo "test 2"