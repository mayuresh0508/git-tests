name: Test Print

on:
  push:
  workflow_dispatch:

jobs:
  print-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print test 1
        run: echo "test 1"
